<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mypet.mapper.AlbumMapper">
	
	 <select id="getList" resultType="com.mypet.domain.AlbumVO">
	 	<![CDATA[
	 		select * from album where a_no > 0
	 		]]>
	 </select>
	
	<insert id="insert">
		insert into album(a_no, a_title, a_content, a_photo)
		values (seq_album.nextval, #{a_title}, #{a_content}, #{a_content})
	</insert>
	
	<select id="read" resultType="com.mypet.domain.AlbumVO">
		select * from album where a_no = #{a_no}
	</select>
	
	<delete id="delete" >
		delete album where a_no = #{a_no}
	</delete>
	
	<update id="update">
		update album
		set a_title = #{a_title},
		a_content = #{a_content},
		a_photo = #{a_photo},
		a_udate = sysdate
		where a_no = #{a_no}
	</update>


</mapper>